# Resolver los peers; nunca ocultar conflictos con legacy-peer-deps.
# Los peers nativos opcionales de Fiber no se instalan en este cliente web.
# AJV 8 y los tipos de React se declaran en client para satisfacer sus peers;
# ESLint conserva su propia versión compatible de AJV 6.
strict-peer-deps=true

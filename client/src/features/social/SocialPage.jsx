import { useEffect, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import axios from "axios";
import CreatePost from "./CreatePost";
import { getToken } from "../../services/authStorage";
import { API_URL } from "../../services/apiClient";
const API = API_URL;
function auth() { const value = getToken(); return value ? { headers: { Authorization: `Bearer ${value}` } } : {}; }
function date(value) { return value ? new Date(value).toLocaleString("es-MX", { dateStyle: "medium", timeStyle: "short" }) : ""; }
export default function SocialPage() { const navigate = useNavigate(); const [posts, setPosts] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [liking, setLiking] = useState(""); const [commenting, setCommenting] = useState(""); const [commentText, setCommentText] = useState({}); const [openComments, setOpenComments] = useState({}); async function loadPosts() { setLoading(true); setError(""); try { const response = await axios.get(`${API}/posts`, auth()); setPosts(Array.isArray(response.data?.posts) ? response.data.posts : []); } catch (e) { setError(e.response?.data?.error || "No se pudieron cargar las publicaciones."); } finally { setLoading(false); } } useEffect(() => { loadPosts(); }, []); async function likePost(id) { if (liking) return; setLiking(id); try { const r = await axios.post(`${API}/posts/${id}/like`, {}, auth()); setPosts(items => items.map(p => p._id === id ? { ...p, liked: r.data.liked, likesCount: r.data.likesCount } : p)); } catch (e) { setError(e.response?.data?.error || "No se pudo actualizar el like."); } finally { setLiking(""); } } async function commentPost(event, id) { event.preventDefault(); const content = (commentText[id] || "").trim(); if (!content || commenting) return; setCommenting(id); try { const r = await axios.post(`${API}/posts/${id}/comments`, { content }, auth()); if (r.data?.post) setPosts(items => items.map(p => p._id === id ? r.data.post : p)); setCommentText(items => ({ ...items, [id]: "" })); } catch (e) { setError(e.response?.data?.error || "No se pudo publicar el comentario."); } finally { setCommenting(""); } } async function sharePost(post) { const url = `${window.location.origin}/post/${post._id}`; try { if (navigator.share) await navigator.share({ title: "Publicación en Kronos", text: post.content, url }); else { await navigator.clipboard.writeText(url); window.alert("Enlace copiado"); } } catch (e) { if (e.name !== "AbortError") setError("No se pudo compartir la publicación."); } } if (loading) return <section className="page"><div className="k-surface k-feed-state"><span className="k-skeleton" /><span className="k-skeleton k-skeleton-wide" /><span className="k-skeleton" /></div></section>; return <section className="page k-feed-page"><header className="k-page-header"><div><p className="k-eyebrow">KRONOS / SOCIAL</p><h1>Tu feed</h1><p>Publicaciones reales de tu comunidad.</p></div><button className="k-button k-button-secondary" type="button" onClick={loadPosts}>Actualizar</button></header>{error && <p className="k-state k-state-error" role="alert">{error}</p>}<CreatePost onCreated={post => setPosts(items => [post, ...items])} /><div className="k-feed-list">{posts.length === 0 ? <div className="k-empty-state"><h2>Aún no hay publicaciones</h2><p>Comparte la primera idea de tu comunidad.</p><button className="k-button k-button-primary" type="button" onClick={() => navigate("/create")}>Crear publicación</button></div> : posts.map(post => { const comments = Array.isArray(post.comments) ? post.comments : []; return <article className="k-post" key={post._id}><header className="k-post-header"><Link className="k-avatar" to={post.author?.username ? `/profile/${post.author.username}` : "/profile"}>{post.author?.displayName?.slice(0, 1) || "K"}</Link><div><Link className="k-post-author" to={post.author?.username ? `/profile/${post.author.username}` : "/profile"}>{post.author?.displayName || post.author?.username || "Usuario"}</Link><p>@{post.author?.username || "kronos"} · {date(post.createdAt)}</p></div></header><Link className="k-post-content" to={`/post/${post._id}`}><p>{post.content}</p></Link><div className="k-post-actions"><button type="button" className={post.liked ? "is-liked" : ""} onClick={() => likePost(post._id)} disabled={liking === post._id}>{post.liked ? "Me gusta" : "Like"} · {post.likesCount || 0}</button><button type="button" onClick={() => setOpenComments(items => ({ ...items, [post._id]: !items[post._id] }))}>Comentar · {comments.length}</button><button type="button" onClick={() => sharePost(post)}>Compartir</button></div>{openComments[post._id] && <div className="k-comments"><form onSubmit={e => commentPost(e, post._id)}><input value={commentText[post._id] || ""} onChange={e => setCommentText(items => ({ ...items, [post._id]: e.target.value }))} placeholder="Escribe un comentario" maxLength={1000} /><button className="k-button k-button-primary" type="submit" disabled={commenting === post._id}>{commenting === post._id ? "Enviando..." : "Enviar"}</button></form>{comments.map(c => <div className="k-comment" key={c._id || `${c.user?._id}-${c.content}`}><strong>{c.user?.displayName || c.user?.username || "Usuario"}</strong><span>{c.content}</span></div>)}</div>}</article>; })}</div></section>; }
